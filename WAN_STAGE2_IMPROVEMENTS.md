# WAN Stage 2 改进训练方案

## 问题回顾

您的 Stage 2 训练出现严重过拟合：
- 训练/测试 Loss 比值：**17.7倍**
- 训练/测试 PSNR 差距：**5.02 dB**
- 模型在训练集上表现优秀，但测试集性能很差

## 改进措施对比

### 原始 WAN Stage 2 训练 vs 改进版本

| 参数 | 原始版本 | 改进版本 | 改进说明 |
|------|---------|---------|---------|
| **学习率** | 5e-6 | **2e-6** ⭐⭐⭐⭐ | 降低60%，减少过拟合 |
| **梯度裁剪** | 1.0 | **0.5** ⭐⭐⭐ | 更严格，稳定训练 |
| **感知损失权重** | 0.03 | **0.05** ⭐⭐⭐⭐ | 提高67%，增强泛化 |
| **数据线程** | 2 | **4** ⭐⭐ | 加快数据加载 |
| **训练周期** | 30 | **180** ⭐⭐⭐⭐⭐ | 完整训练，充分收敛 |
| **Batch Size** | 1 | **1** | 保持不变（显存限制） |
| **GPU数量** | 8 | **8** | 保持不变 |

## 为什么这些改进有效？

### 1. 降低学习率 (5e-6 → 2e-6) ⭐⭐⭐⭐

**原因**：
- 学习率过高导致模型在训练集上过度优化
- 降低学习率使模型更新更平滑，不容易"记忆"训练数据

**预期效果**：
- 训练/测试差距从 17.7x 降至 < 3x
- 测试 PSNR 提升 1-2 dB

### 2. 更严格的梯度裁剪 (1.0 → 0.5) ⭐⭐⭐

**原因**：
- 限制每次参数更新的幅度
- 防止梯度爆炸和训练不稳定

**预期效果**：
- 训练曲线更平滑
- 减少 Loss 突然上升的情况

### 3. 增加感知损失权重 (0.03 → 0.05) ⭐⭐⭐⭐

**原因**：
- LPIPS 感知损失更关注语义特征，而非像素级拟合
- 提高感知损失权重可以提升泛化能力

**预期效果**：
- 测试集 LPIPS 降低 0.05-0.10
- 视觉质量提升

### 4. 增加数据线程 (2 → 4) ⭐⭐

**原因**：
- 8卡服务器，2个线程可能成为瓶颈
- 增加线程可以充分利用CPU

**预期效果**：
- 训练速度提升 10-20%
- GPU 利用率提高

### 5. 完整训练周期 (30 → 180) ⭐⭐⭐⭐⭐

**原因**：
- 30 个 epoch 可能不足以充分训练
- 180 个 epoch 确保模型充分收敛

**预期效果**：
- 达到更好的性能上限
- 测试 PSNR 可能提升 2-3 dB

## 与原始训练的核心区别

### ❌ 原始训练的问题

```bash
# 原始命令
--learning-rate 5e-6      # 学习率过高
--clip_max_norm 1.0       # 梯度裁剪不够严格
--perceptual_weight 0.03  # 感知损失权重较低
--num-workers 2           # 数据加载可能成为瓶颈
--epochs 30               # 训练不充分
```

**结果**：
- ❌ 训练Loss: 16.731
- ❌ 测试Loss: 0.945 (17.7倍差距)
- ❌ 测试PSNR: 23.18 dB

### ✅ 改进训练的优势

```bash
# 改进命令
--learning-rate 2e-6      # 降低60%，减少过拟合
--clip_max_norm 0.5       # 更严格的梯度控制
--perceptual_weight 0.05  # 提高67%，增强泛化
--num-workers 4           # 充分利用8卡服务器
--epochs 180              # 完整训练周期
```

**预期结果**：
- ✅ 训练/测试Loss比值: < 3.0x (改善 83%)
- ✅ 测试PSNR: 25-26 dB (+2-3 dB)
- ✅ 测试LPIPS: 0.30-0.35 (改善)

## 使用方法

### 在 tmux 中运行

```bash
# 1. 进入 tmux 会话
tmux attach -t dc

# 2. 切换到项目目录
cd /home/serverdn/hdd-0/wjw/dcvc_rt_g2

# 3. 运行改进的 WAN Stage 2 训练
bash run_wan_stage2_improved.sh

# 4. 退出 tmux 但保持训练运行
# 按 Ctrl+B，然后按 D
```

### 监控训练

```bash
# 查看日志
tail -f pretrained/DMC_WAN/1/*.log

# 重新连接 tmux
tmux attach -t dc
```

## 预期训练时间

- **8卡服务器**：约 3-4 天
- **每个 epoch**：约 30-40 分钟
- **总计**：180 epochs × 35 分钟 ≈ 105 小时

## 监控指标

### 健康训练的标志

| Epoch | 训练PSNR | 测试PSNR | 训练/测试差距 | 状态 |
|-------|---------|---------|--------------|------|
| 10 | 24 dB | 22 dB | 2 dB | ✅ 正常 |
| 30 | 26 dB | 24 dB | 2 dB | ✅ 正常 |
| 60 | 27 dB | 25 dB | 2 dB | ✅ 正常 |
| 100 | 28 dB | 26 dB | 2 dB | ✅ 正常 |
| 150 | 29 dB | 27 dB | 2 dB | ✅ 正常 |

### 异常标志

| 现象 | 可能原因 | 解决方案 |
|------|---------|---------|
| 训练/测试差距 > 3 dB | 仍然过拟合 | 进一步降低学习率到 1e-6 |
| Loss 突然上升 | 梯度爆炸 | 降低 clip_max_norm 到 0.3 |
| PSNR 不上升 | 学习率过低 | 适当提高到 3e-6 |

## 与 train_vd_phase_2_improved.py 的区别

### train_vd_phase_2_improved.py（不适用于 WAN）

- ❌ 不支持 WAN VAE
- ❌ 不支持 latent mode
- ❌ 不支持 adaptation layers
- ✅ 支持多测试集验证
- ✅ 支持早停机制
- ✅ 支持 L2 正则化

### run_wan_stage2_improved.sh（适用于 WAN）

- ✅ 支持 WAN VAE
- ✅ 支持 latent mode
- ✅ 支持 adaptation layers
- ✅ 应用降低学习率
- ✅ 应用严格梯度裁剪
- ✅ 应用增强感知损失
- ❌ 暂不支持多测试集（需要修改 train_vd_phase_1_wan.py）
- ❌ 暂不支持早停机制（需要修改 train_vd_phase_1_wan.py）

## 进一步改进（可选）

如果使用改进的脚本后仍然过拟合，可以：

### 1. 进一步降低学习率

```bash
# 修改 run_wan_stage2_improved.sh
LEARNING_RATE=1e-6  # 从 2e-6 降至 1e-6
```

### 2. 更严格的梯度裁剪

```bash
CLIP_MAX_NORM=0.3  # 从 0.5 降至 0.3
```

### 3. 进一步增加感知损失

```bash
PERCEPTUAL_WEIGHT=0.08  # 从 0.05 增至 0.08
```

### 4. 添加多测试集支持（需要修改代码）

修改 `train_vd_phase_1_wan.py` 以支持多测试集验证（类似 `train_vd_phase_2_improved.py`）。

## 总结

### 核心改进

1. ⭐⭐⭐⭐ **降低学习率 60%** - 最重要的改进
2. ⭐⭐⭐⭐ **增加感知损失 67%** - 提升泛化能力
3. ⭐⭐⭐⭐⭐ **完整训练周期** - 充分收敛
4. ⭐⭐⭐ **更严格梯度裁剪** - 稳定训练
5. ⭐⭐ **增加数据线程** - 提升效率

### 预期效果

- ✅ 训练/测试差距：17.7x → < 3x (83%改善)
- ✅ 测试PSNR：23.18 dB → 25-26 dB (+2-3 dB)
- ✅ 过拟合程度：严重 → 轻微

### 立即开始

```bash
cd /home/serverdn/hdd-0/wjw/dcvc_rt_g2
bash run_wan_stage2_improved.sh
```

---

**创建时间**: 2026-02-01  
**版本**: 1.0
